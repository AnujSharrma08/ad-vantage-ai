/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['anuj-nextjs-s3-test.s3.eu-north-1.amazonaws.com'],
  },
};

export default nextConfig;

